[package]
name = "pythos-user-arp-probe"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true

[dependencies]
pythos-shared = { path = "../../../shared" }
pythos-user-link-layer-probe = { path = "../link-layer" }

[lib]
path = "src/lib.rs"

[[bin]]
name = "pythos-user-arp-probe"
path = "src/main.rs"
