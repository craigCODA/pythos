# Phase 13.5 Slices 3 and 4 implementation map

> Execution: subagent-driven-development, with owner-authorized parallel workers
> in separate Git worktrees. This living map is the progress and integration
> authority; ADR 0092 records only new boundary decisions.

**Goal:** retain ADR 0089 input routing in the ring-3 session and deliver its
read-only snapshots to a capability-checked presenter while that session lives.

**Scope:** Slices 3 and 4 only. Preserve Slice 2, PythTIG/input V1, default boot,
existing markers and physical evidence boundaries. No Slice 5 or remote writes.

**Baseline:** main 8c2d3a83cef5388dd091f0acc1676bebfe8bbc44. Existing eight
documentation edits on main are owner-requested and must not be reverted.
Integration worktree: .worktrees/phase13-5-viewing-integration.

**Architecture:** shared pure policy -> retained user owner -> scalar snapshot
syscall -> bounded kernel presenter. See ADRs 0089-0092. Agents may not alter
shared contracts silently. Parent owns cross-task decisions and integration.

**Validation:** behavior-first host tests with observed red/green results;
unchanged Slice 2 and Viewing predecessor regressions; strict formatting and
scoped Clippy; seven-event, two-boot QEMU acceptance with three live screenshots,
exact serial ordering, containment and no writes. Default boot and storage
acceptance must remain passing. Do not report acceptance until fresh evidence.

## Task 1: Shared policy and retained Viewing owner

**Owner:** routing worker, separate worktree. You are not alone in this codebase;
do not revert others' changes. Do not spawn subagents. Read AGENTS.md, SAS/TDD
and ADRs 0089-0092. The owner's explicit invocation authorizes Slices 3 and 4
despite older checked-in milestone text.

Own shared/src/input_types.rs, shared/src/session_controls.rs,
shared/src/viewing/*, core/src/input_drivers.rs, core/src/input_events.rs,
core/src/session_controls.rs, core/src/viewing/*,
user/session-runtime/src/session_viewing.rs and its export in src/lib.rs.
Add needed shared/src/lib.rs exports (other workers add distinct exports).
No Cargo feature, syscall, framebuffer or runtime main changes in this task.

1. Move pure types and existing tested ADR 0089 policy into shared; leave core
   compatibility re-exports so all prior consumers use exactly one implementation.
2. Add SessionViewing::new(ViewingExtent) and observe(SessionInputEventV1)
   returning Result<SessionViewingReceipt, SessionViewingError>. Receipt exposes
   sequence, continuity, command, motion_route and snapshot. Also expose snapshot
   and traversal intent count as read-only session evidence. Use existing types.
3. Implement ADR 0092 checked decoding, gap reset, wrapping continuity and poisoned
   recovery after malformed/discontinuous input. No duplicate controls or queue.
4. Test every tag, malformed shape/range, gap at partial/active state, wraparound,
   duplicate/skips, exclusive routing, raw buttons, idempotence, clamping and
   state retained across graph-local work. Literal expected behavior, not source
   checks or test-only production branches. Observe failing tests before fixes.
5. Run relevant Rust tests, cargo fmt check and old Viewing tests. Commit owned
   changes only, report SHA, commands/results and any limitations. Do not merge.

## Task 2: Capability-gated presentation bridge

**Owner:** presentation worker, separate worktree. You are not alone; preserve
others' edits, no subagents. Read AGENTS.md, SAS/TDD and ADRs 0089-0092. Explicit
owner invocation authorizes Slices 3 and 4, not default cutover.

Own shared/src/session_viewing_abi.rs (scalar transport and BootstrapV1 only),
core/src/session_presentation.rs, core/src/framebuffer.rs, core/src/syscall.rs.
Add module declaration in core/src/main.rs and shared export as necessary;
parent/integration worker own other feature/Cargo/profile changes. Do not edit
session_runtime_probe.rs, user runtime or scripts.

1. Define and test exact ADR 0092 syscall/resource/bootstrap ABI, reserved fields,
   flags, coordinates and revision rules. Do not repurpose existing ABI fields.
2. Add capability/active-caller-gated syscall and a bind-once presentation service,
   storing only copied framebuffer metadata, session owner and accepted snapshot.
   Public integration seam: bind(ServiceId, PythFramebufferInfo, ViewingExtent)
   before user entry, and syscall dispatch for the scalar request. Refine names
   with parent if needed. Only SEND on the separate resource authorizes updates.
3. Initialize the fixed viewport before entry; recurrent updates erase the old
   FocusMark footprint and draw the new using existing four-L geometry. Validate
   everything before rendering; rejection/render failure preserves accepted state
   and old pixels. Bounded work, no font/device/input dependency, documented unsafe
   invariants. No full-frame redraw on each syscall and no deferred-only mailbox.
4. Test forged/stale/wrong-holder/resource/rights, unbound calls, malformed fields,
   revision errors/exhaustion and atomic rejection with literal pixel tests for
   inactive, activation, movement, clipping and old-mark removal. Observe red/green.
5. Gate hardware binding appropriately for session-viewing-probe or test. New
   feature will be added by integration; a temporary local feature addition may
   be used to compile but leave it uncommitted and report it. Preserve old profile.
6. Commit owned changes; report SHA, tests, required integration seam and evidence.

## Task 3: Retained profile and bootstrap integration

**Owner:** integration worker, separate worktree. You are not alone, no subagents.
Read AGENTS.md, SAS/TDD and ADRs 0089-0092. The user invoked Slices 3 and 4; do
not implement Slice 5, alter frozen graph/input ABI or weaken old acceptance.

Own core/src/session_runtime_probe.rs, core/src/main.rs profile wiring,
core/Cargo.toml, user/session-runtime/Cargo.toml, user/session-runtime/src/main.rs,
new user runtime orchestration module, shared/src/session_viewing_result.rs,
scripts/build-session-runtime.py and tests/test_session_runtime_boundary.py.
Coordinate additive module exports with other workers; no framebuffer/syscall
service changes or new QEMU harness (parent owns harness).

1. Add opt-in kernel session-viewing-probe -> session-runtime-probe and user
   session-viewing feature. Preserve default coordinator and old profile outputs.
   Add builder --features argument with unchanged default and isolated target dir.
2. Reuse retained launch/admission/graph execution without copy-pasting the entire
   Slice 2 implementation. Add separately validated bootstrap extension at offset
   2048, and a separately versioned result extension at offset 2048. Propose exact
   result layout to parent immediately for ADR recording, then implement tests.
3. Bind presentation and grant separate SEND only to runtime. Explicitly map
   framebuffer supervisor-only/NX into retained USER root; validate endpoints and
   authority exclusion. Do not change hardware-neutral kernel-root policy.
4. Construct SessionViewing once, route every delivered input through it, submit
   initial and per-event snapshots via scalar syscall. Seven-event sequence and
   graph invocation checkpoints are ADR 0092. Keep two immutable fixtures and
   independently validated fresh interpreter results; fail closed into recovery.
5. Coordinate deterministic COM2 stages with parent: VIEWING:WAIT_EVENT:0..6
   immediately after initial/prior-event drawn acknowledgement, and DRAWN:REV:0..7
   after successful submissions. Prefix PYTHOS:USER:SESSION_VIEWING:. Include
   routing/activation and invocation evidence without relying on user success
   markers alone. Core validates records and emits final acceptance only on truth.
6. Test malformed extension/result, lifecycle, retained recognizer across fresh
   graph reset, state counters and default profile preservation. Update old
   boundary tests deliberately for authorized profile, no hiding imports/aliases.
7. Commit owned changes; report SHA, tests, exact marker contracts, build commands
   and required cross-worker dependencies. Compilation may await worker commits;
   do not replace their modules with competing implementations.

## Task 4: Independent acceptance and branch review

**Owner:** parent, then fresh reviewer. Parent integrates worker commits in order
1, 2, 3, resolves additive exports and runs behavior tests before QEMU.

Build scripts/test-session-viewing-probe.py using existing qemu_probe_support
process/COM1/COM2/QMP helpers. Own new behavioral harness tests. Preserve existing
harnesses and CLI contracts. Use separate port 4593 and per-worktree artifacts.
Inject seven real PS/2 events, capture inactive/active/moved frames BEFORE final
Enter, require independent exact pixels/markers, terminal truth, two boots and
no storage changes. Include malformed transcript and timeout/cleanup tests.

Run baseline and focused suites, default boot, persistent storage, prior session
runtime and Viewing acceptance, new acceptance. Use explicit installed Python;
bare python currently resolves to the Windows Store alias. If pytest is absent,
use a task-local/uv-managed environment, not a machine-global installation.
Review full diff with a fresh most-capable agent; fix confirmed findings and
rerun affected gates. Update this map and relevant current-state docs accurately.
Do not claim production wait/wakeup, physical hardware support or Slice 5.

## Progress

- Shared contract: ADR 0092 implemented. Default boot and Slice 5 remain untouched.
- Baseline: 1008 Rust tests; 148 Python tests and 119 subtests passed. An isolated
  uv environment supplies pytest; machine-global Python configuration is unchanged.
- Tasks 1-3 implemented in isolated branches, integrated and independently reviewed.
  Routing review fixes are incorporated through e037554; presentation and runtime
  reviews found no blocking defects. Three worker reports and review packages are
  retained in this worktree's ignored, plan-specific SDD directory.
- Task 4 live proof passed: two complete QEMU boots, eight synchronous revisions
  per boot, three pre-exit pixel captures per boot, retained graph state and fresh
  invocation-local tables, reboot reset, exact serial ordering, no storage writes
  and complete process-tree cleanup. Evidence: target/session-viewing-probe/
  acceptance-2dkv6wg8/. Both runner logs contain QEMU_OUTCOME success; the harness
  concluded SESSION_VIEWING_PROBE_TEST_OK.
- Earlier acceptance-jqrqvy6_ and acceptance-f889u7fh are retained failed HARNESS
  runs, not accepted boots. They exposed final-key-up-after-exit and Windows EOF/RST
  handling. Regressions were observed red/green; 27 independent harness tests pass.
- Strict Clippy for both new profiles passes without suppressions. Existing host
  test warnings were present at baseline, separate from introduced lint issues.
- CI now runs the new unit/lint/oracle/live gates in milestone_acceptance only;
  six workflow tests and 20 duplicate-injection subtests pass. No remote CI run
  or publication has been requested or claimed.
- Final workspace gates passed: 1029 Rust tests; 176 Python tests and 148
  subtests; formatting and diff checks; strict Clippy for core verify, core
  session-runtime-probe, core session-viewing-probe, user runtime default and
  user runtime session-viewing profiles. Full suite logs are retained under
  target/viewing-workspace-tests.log and target/viewing-python-tests.log.
- All seven predecessor QEMU regressions passed: boot, persistent storage,
  normal fast boot, Viewing input, retained runtime fault, retained runtime,
  and input bridge. Commands, exact terminal evidence and log hashes are in
  the ignored plan-specific SDD regressions-report.md; logs are retained under
  target/task3-regression-*.log. The shared QMP port is released.
- Whole-branch review through c329945 found no Critical or Important issues.
  Current absence inventories were reconciled after review; the minor host
  fixture observation below is explicitly deferred.
- Slices 3 and 4 are locally accepted in their bounded opt-in QEMU profile.
  Stop before Slice 5. The owner chose to retain the branch locally: no merge,
  push, remote CI claim, physical deployment or worktree cleanup.
- Deferred minor coverage observation: the orchestration mock does not assemble
  graph outputs into its copied terminal record. The real runtime assembly and
  kernel validator are exercised by the two-boot QEMU proof; a dedicated host
  serialization test would improve diagnostics but is not a production defect.
